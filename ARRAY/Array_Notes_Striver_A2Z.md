# Striver A2Z DSA: Arrays Notes

Source sheet: https://takeuforward.org/dsa/strivers-a2z-sheet-learn-dsa-a-to-z

Important: I cannot safely copy Striver's exact code line-by-line from takeUforward. These notes follow the same Striver-style flow and approaches, but the C++ code is freshly written and compile-friendly.

## Files

| Part | File |
| --- | --- |
| Easy array problems | `arrays_notes/01_easy_arrays.md` |
| Medium array problems | `arrays_notes/02_medium_arrays.md` |
| Hard/final array problems | `arrays_notes/03_hard_arrays.md` |
| Compiled optimal library | `arrays_striver_a2z.cpp` |

## Required Order Followed

Each problem is written like this:

1. Problem statement
2. Brute force approach + code + time/space complexity
3. Better approach + code + time/space complexity, if meaningful
4. Optimal approach + code + time/space complexity

## Compile Check

The reusable C++ file compiles with:

```bash
g++ -std=c++17 -Wall -Wextra -pedantic arrays_striver_a2z.cpp -o arrays_striver_a2z
```
